# Logistic-Regression-Classification
This project demonstrates Logistic Regression Classification using Python and Scikit-learn. It includes data preprocessing, feature selection, train-test splitting, model training, evaluation with accuracy and confusion matrix, threshold tuning, sigmoid function visualization, and feature importance analysis.
